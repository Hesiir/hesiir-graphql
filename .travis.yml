language: node_js
node_js:
  - "node"
  - "iojs"
cache:
  yarn: true
install:
  - yarn
before_script:
  - yarn global add babel-cli typescript typings
  - typings install
  - tsc ./src/* --outDir ./tsbuild/
script:
  - yarn run start